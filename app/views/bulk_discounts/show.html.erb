<h2><%= "Bulk Discount #{@bulk_discount.id}" %></h2>
Quantity Threshold: <%="#{@bulk_discount.quantity_threshold}" %><br>
Percentage: <%= number_to_percentage("#{@bulk_discount.percentage}", precision: 0) %>

<%= link_to "Edit Discount", edit_merchant_bulk_discount_path(@merchant, @bulk_discount) %>
